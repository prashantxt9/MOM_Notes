{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "git pull --ff-only"
      }
    },
    {
      "method": "shell.run",
      "params": {
        "conda": ".pinokio",
        "message": "python -m pip install --upgrade -e \".[capture,transcription,diarization,nvidia-asr,pyannote-diarization]\" && python -m pip install \"huggingface-hub>=0.27,<2\" && python -m pip install \"llama-cpp-python>=0.3.8,<1\" --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/{{os.platform() === 'darwin' ? 'metal' : 'cpu'}} && python -m pip check"
      }
    }
  ]
}
